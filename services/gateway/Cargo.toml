[package]
name = "gateway"
version = "0.1.0"
edition = "2024"

[dependencies]

[profile.release]
overflow-checks = true
strip = true

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
unwrap_used = "deny"
expect_used = "deny"
panic = "deny"
indexing_slicing = "deny"
float_arithmetic = "deny"
